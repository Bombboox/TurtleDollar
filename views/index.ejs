<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Turtle Dollar: Menu</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <nav>
            <ul class="nav__buttons">
                <% if(locals.user) { %>
                    <a href="/profile" class="username_text clickable"><%=locals.user.name.toUpperCase()%></a>
                <% } %>
                <a href="/" class="clickable">Menu</a>   
            </ul>
        </nav>
    </header>

    <footer>
        <nav>
            <ul class="nav__buttons">
                <span>&copy; 2023-2023 Turtle Dollar Co. | All Rights Reserved</span>
            </ul>
        </nav>
    </footer>

    <div id="swup" class="transition-fade">
        <div id="menu-items">
            <% if(!locals.user) { %>
                <a href="/register" class="menu-item">Register</a>
                <a href="/login" class="menu-item">Login</a>
                <a href="/about" class="menu-item">About</a>
            <% } else { %>
                <a href="/surveys" class="menu-item highlight">Surveys</a>
                <a href="/payout" class="menu-item">Payout</a>
                <a href="/about" class="menu-item">About</a>
                <a href="/logout" class="menu-item">Logout</a>
            <% } %>

        </div>
        <div id="welcome-text">
            <% if(!locals.user) { %>
                <p>Welcome to</p>
                <p class="flashing">Turtle Dollar</p>
            <% } else { %>
                <p>Welcome</p>
                <p class="flashing"><%=locals.user.name%></p>
            <% } %>
        </div>
        <div class="page-background" id="menu-background"></div>
    </div>
</body>
</html>